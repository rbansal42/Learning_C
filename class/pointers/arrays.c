/* 
In RAM, we have blocks/sectors of data


In C, we get a somewhat direct access to RAM (Available Memory). 
However, other languages do not do that, and automatically assign variables.

*/

#include <stdio.h>

