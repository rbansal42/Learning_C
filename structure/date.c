#include <stdio.h>

struct date {
    int year;
    int mon;
    int day;
};      // Why is this semicolon there?!??

int fdiff(struct date d1, struct date d2);

int main() {

    struct date
    {
        start, end;
    };
    
    scanf()
}